# 🖥️ 本地開發指南

## ✅ 專案狀態確認

✅ **專案已可正常在本地運行！**

## 🚀 啟動步驟

### 1. 安裝依賴
```bash
npm install
```

### 2. 啟動開發服務器
```bash
npm run dev
```

### 3. 開啟瀏覽器
訪問：http://localhost:3000

## 🔧 開發環境特點

### **自動配置**
- ✅ **端口**：自動使用 3000
- ✅ **熱重載**：檔案修改自動刷新
- ✅ **Mock 用戶**：開發模式自動登入
- ✅ **本地存儲**：使用 localStorage，無需資料庫

### **可選配置**
如需使用 AI 功能，創建 `.env.local`：
```env
API_KEY=your-gemini-api-key-here
```

## 📁 開發模式 vs 生產模式

| 功能 | 開發模式 (npm run dev) | 生產模式 (部署後) |
|------|----------------------|------------------|
| **用戶認證** | Mock 用戶 (自動登入) | Firebase Auth (Google 登入) |
| **資料存儲** | localStorage | Firestore |
| **AI 服務** | 需要 API_KEY | 需要 API_KEY |
| **端口** | 3000 | 由雲端平台指定 |

## 🛠️ 開發工具

### **可用指令**
```bash
npm run dev     # 開發模式
npm run build   # 建構生產版本
npm run preview # 預覽建構結果
npm start       # 生產模式啟動 (需先建構)
```

### **專案結構**
```
src/
├── components/          # React 組件
├── services/           # API 服務
│   ├── authService.ts  # 身份驗證
│   ├── storageService.ts # 資料存儲
│   └── geminiService.ts  # AI 服務
├── types.ts           # TypeScript 型別
└── config.ts          # 配置檔案
```

## 🔍 故障排除

### **常見問題**

1. **端口被佔用**
   ```bash
   # Windows
   netstat -ano | findstr :3000
   
   # 或更改端口 (vite.config.ts)
   server: { port: 3001 }
   ```

2. **模組找不到**
   ```bash
   rm -rf node_modules package-lock.json
   npm install
   ```

3. **TypeScript 錯誤**
   ```bash
   npx tsc --noEmit
   ```

### **檢查服務器狀態**
```bash
# 檢查端口是否監聽
netstat -ano | findstr :3000

# 測試 HTTP 回應
curl -I http://localhost:3000
```

## 📝 開發注意事項

- **開發模式**使用假的用戶資料，生產模式需要真實登入
- **API Key** 只在使用 AI 功能時必需
- **Firebase 配置**已硬編碼在 `config.ts`，本地開發無需額外設定
- **資料隔離**確保每個用戶只看到自己的資料
